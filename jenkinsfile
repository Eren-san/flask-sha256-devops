pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker-compose build'
            }
        }
        stage('Up'){
            steps {
                sh 'docker-compose up -d'
            }
        }
        stage('Test'){
            steps {
                sh 'pytest'
            }
        }
    }
}
